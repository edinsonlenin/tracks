import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SingupScreen = props => {
    return <Text style={{fontSize: 48}}>Singup Screen</Text>;
};

const styles = StyleSheet.create({

});

export default SingupScreen;